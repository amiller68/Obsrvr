import * as React from 'react';
import { StyleSheet, Text, View, TextInput } from 'react-native';

export default function init(updateRouterData: (item_name: string, item_value: any) => void) {
    return (
        <View>
            <Text>Hi there!</Text>
        </View>
    )
}
